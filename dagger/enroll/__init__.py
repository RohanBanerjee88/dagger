"""Enrollment: top-K solo clips -> phi -> mean embedding ``e_bar_i`` + variance ``V_i``.

Not yet implemented. Arrives in Phase 1 (embeddings) and Phase 3 (the ``V_i``
enrollment-rejection guard that catches solo regions that were secretly
overlaps). See CLAUDE.md §5.
"""
