# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Secrets / local env (data root, corpus credentials)
.env

# Local corpus data — never commit audio; it lives on the mounted volume.
# Root-anchored so it only ignores a top-level data/ dir, NOT the dagger/data
# Python package.
/data/

# Personal study notes — not part of the repo. Exception: theory.pdf is part
# of the target layout (CLAUDE.md §4) and must stay committable.
/docs/*
!/docs/theory.pdf
